<div class="row">
  <div class="span8 well">
    <div class="span6">
      <h4><%= link_to collection.name, user_collection_path(user, collection) %></h4>
      <p><%= collection.description %></p>
    </div>
    <div class="pull-right">
      <%= link_to 'Delete', user_collection_path(user, collection), method: :delete, data: { confirm: "Are you sure?" }, class: "btn" %>
    </div>
  </div>
</div>
